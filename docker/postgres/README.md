# PostgreSQL

Local development uses the official PostgreSQL Alpine image through Docker Compose.

```bash
docker compose up -d postgres
```

Use `.env.example` for local database name, username, password, and `DATABASE_URL`.
