# SQL Server

Local development uses the official Microsoft SQL Server 2022 container.

```bash
docker compose up -d sqlserver
```

Use `.env.example` as the password reference. The password must satisfy SQL Server complexity rules.
