# Albums Module

Owns album CRUD, ordering, cover media, visibility, password protection, download controls, and metadata batch updates.

Album privacy uses `public`, `unlisted`, and `private`. Public albums can appear in discovery/search, unlisted albums require a direct link, and private albums remain owner/admin scoped.
